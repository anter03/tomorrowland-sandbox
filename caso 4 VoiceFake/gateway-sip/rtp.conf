; ============================================================
;  RTP Configuration — Range porte limitato per Docker
; ============================================================

[general]
rtpstart=10000
rtpend=10100
